const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001';

class ApiClient { private baseUrl: string; private apiKey?: string; constructor(baseUrl: string = API_BASE) { this.baseUrl = baseUrl; } setApiKey(key: string) { this.apiKey = key; } private async request<T>(endpoint: string, options: RequestInit = {}): Promise<T> { const headers: HeadersInit = { 'Content-Type': 'application/json', ...options.headers }; if (this.apiKey) (headers as Record<string, string>)['Authorization'] = `Bearer ${this.apiKey}`; const response = await fetch(`${this.baseUrl}${endpoint}`, { ...options, headers, next: { revalidate: 60 } }); if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Request failed' })); throw new Error(error.message || `HTTP ${response.status}`); } return response.json(); } getTeams(params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<{ data: any[]; total: number }>(`/api/teams?${search}`); } getTeam(id: string) { return this.request<any>(`/api/teams/${id}`); } getTeamForm(id: string, limit: number = 5) { return this.request<any>(`/api/teams/${id}/form?limit=${limit}`); } getTeamSeasonStats(teamId: string, seasonId: string) { return this.request<any>(`/api/teams/${teamId}/season-stats?seasonId=${seasonId}`); } getHeadToHead(teamId1: string, teamId2: string, limit: number = 10) { return this.request<any>(`/api/teams/${teamId1}/h2h/${teamId2}?limit=${limit}`); } getPlayers(params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<{ data: any[]; total: number }>(`/api/players?${search}`); } getPlayer(id: string) { return this.request<any>(`/api/players/${id}`); } getPlayerSeasonStats(playerId: string, seasonId: string, competitionId?: string) { const search = new URLSearchParams({ seasonId, ...(competitionId && { competitionId }) }).toString(); return this.request<any>(`/api/players/${playerId}/season-stats?${search}`); } getPlayerCareer(playerId: string) { return this.request<any>(`/api/players/${playerId}/career`); } comparePlayers(playerIds: string[], seasonId?: string, competitionId?: string) { const search = new URLSearchParams({ ids: playerIds.join(','), ...(seasonId && { seasonId }), ...(competitionId && { competitionId }) }).toString(); return this.request<any>(`/api/players/compare?${search}`); } getTopPerformers(metric: string, params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<any[]>(`/api/players/top/${metric}?${search}`); } getMatches(params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<{ data: any[]; total: number }>(`/api/matches?${search}`); } getLiveMatches() { return this.request<any[]>(`/api/matches/live`); } getUpcomingMatches(limit: number = 10) { return this.request<any[]>(`/api/matches/upcoming?limit=${limit}`); } getMatch(id: string) { return this.request<any>(`/api/matches/${id}`); } getMatchEvents(matchId: string, params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<any[]>(`/api/matches/${matchId}/events?${search}`); } getShots(params?: Record<string, string>) { const search = new URLSearchParams(params).toString(); return this.request<any[]>(`/api/events/shots?${search}`); } getHeatmap(playerId: string, seasonId?: string, competitionId?: string) { const search = new URLSearchParams({ ...(seasonId && { seasonId }), ...(competitionId && { competitionId }) }).toString(); return this.request<any>(`/api/events/heatmap/${playerId}?${search}`); } getLeagueTable(seasonId: string) { return this.request<any[]>(`/api/analytics/table/${seasonId}`); } getTopScorers(seasonId: string, competitionId?: string, limit: number = 20) { const search = new URLSearchParams({ ...(competitionId && { competitionId }), limit: limit.toString() }).toString(); return this.request<any[]>(`/api/analytics/top-scorers/${seasonId}?${search}`); } getTopAssists(seasonId: string, competitionId?: string, limit: number = 20) { const search = new URLSearchParams({ ...(competitionId && { competitionId }), limit: limit.toString() }).toString(); return this.request<any[]>(`/api/analytics/top-assists/${seasonId}?${search}`); } getPlayerRadar(playerId: string, seasonId: string, competitionId?: string) { const search = new URLSearchParams({ seasonId, ...(competitionId && { competitionId }) }).toString(); return this.request<any[]>(`/api/analytics/radar/${playerId}?${search}`); } searchAll(query: string, limit: number = 5) { return this.request<any>(`/api/search?q=${encodeURIComponent(query)}&limit=${limit}`); } }

export const api = new ApiClient(); export const fetcher = <T>(url: string) => api.request<T>(url);