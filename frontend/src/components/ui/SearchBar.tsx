'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Search, X } from 'lucide-react';
import { useSearch } from '@/hooks/useApi';
import Link from 'next/link';

export function SearchBar() { const [query, setQuery] = useState(''); const [showResults, setShowResults] = useState(false); const router = useRouter(); const { data: searchResults } = useSearch(query); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (query.trim()) { router.push(`/search?q=${encodeURIComponent(query.trim())}`); setShowResults(false); setQuery(''); } }; return ( <div className="relative"> <form onSubmit={handleSubmit} className="relative" role="search"> <label htmlFor="search" className="sr-only">Search teams, players, competitions</label> <div className="relative"> <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" aria-hidden="true" /> <input id="search" type="search" value={query} onChange={(e) => { setQuery(e.target.value); setShowResults(true); }} onFocus={() => setShowResults(true)} onBlur={() => setTimeout(() => setShowResults(false), 200)} placeholder="Search teams, players..." className="w-64 md:w-80 pl-10 pr-10 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent" aria-autocomplete="list" aria-controls="search-results" aria-expanded={showResults && !!searchResults} /> {query && ( <button type="button" onClick={() => setQuery('')} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600" aria-label="Clear search"> <X className="w-5 h-5" /> </button> )} </div> </form> {showResults && searchResults && ( <div id="search-results" className="absolute top-full left-0 right-0 mt-2 bg-white border border-slate-200 rounded-lg shadow-lg overflow-hidden z-50" role="listbox"> {(searchResults.teams?.hits?.length || searchResults.players?.hits?.length || searchResults.competitions?.hits?.length) && ( <> {searchResults.teams?.hits?.length && ( <div className="p-2"> <h4 className="px-3 py-1 text-xs font-semibold text-slate-500 uppercase tracking-wider">Teams</h4> {searchResults.teams.hits.slice(0, 3).map((team: any) => ( <Link key={team.id} href={`/teams/${team.id}`} className="block px-3 py-2 text-sm text-slate-700 hover:bg-slate-50 rounded flex items-center gap-3" onClick={() => setShowResults(false)}> {team.crestUrl && <img src={team.crestUrl} alt="" className="w-6 h-6 rounded" />} <span>{team.name}</span> </Link> ))} </div> )} {searchResults.players?.hits?.length && ( <div className="p-2 border-t border-slate-100"> <h4 className="px-3 py-1 text-xs font-semibold text-slate-500 uppercase tracking-wider">Players</h4> {searchResults.players.hits.slice(0, 3).map((player: any) => ( <Link key={player.id} href={`/players/${player.id}`} className="block px-3 py-2 text-sm text-slate-700 hover:bg-slate-50 rounded flex items-center gap-3" onClick={() => setShowResults(false)}> {player.photoUrl && <img src={player.photoUrl} alt="" className="w-6 h-6 rounded-full" />} <span>{player.displayName}</span> {player.teamName && <span className="text-slate-400 text-xs">{player.teamName}</span>} </Link> ))} </div> )} {searchResults.competitions?.hits?.length && ( <div className="p-2 border-t border-slate-100"> <h4 className="px-3 py-1 text-xs font-semibold text-slate-500 uppercase tracking-wider">Competitions</h4> {searchResults.competitions.hits.slice(0, 3).map((comp: any) => ( <Link key={comp.id} href={`/competitions/${comp.id}`} className="block px-3 py-2 text-sm text-slate-700 hover:bg-slate-50 rounded flex items-center gap-3" onClick={() => setShowResults(false)}> {comp.logoUrl && <img src={comp.logoUrl} alt="" className="w-6 h-6 rounded" />} <span>{comp.name}</span> </Link> ))} </div> )} </> )} {!searchResults.teams?.hits?.length && !searchResults.players?.hits?.length && !searchResults.competitions?.hits?.length && query.length >= 2 && ( <div className="p-4 text-center text-slate-500 text-sm">No results found</div> )} </div> )} </div> ); }